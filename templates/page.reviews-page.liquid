<div class="page-width reviews-page">
  <header class="section-header text-center">
    <h1 class="section-header__title h2">{{ page.title }}</h1>
  </header>
  <div class="grid">
    <div class="grid__item">
      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
